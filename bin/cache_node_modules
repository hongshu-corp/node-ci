CACHE_PATH=/root/cache/yarn
CURRENT_PATH=${CACHE_PATH}/$1
shift

if [ -d ${CURRENT_PATH}/node_modules ]; then
	cp ${CURRENT_PATH}/node_modules . -rf
  eval "$@"
else
  mkdir -p ${CURRENT_PATH}
	eval "$@" && \
	cp node_modules ${CURRENT_PATH}/ -rf
fi

